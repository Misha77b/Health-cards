import { makeStyles } from "@mui/styles";

const MakeStylesAccordion = makeStyles({
    '@global': {
        '.marginNormal': {
            margin: '10px 0'
        }
    }
})

export default MakeStylesAccordion